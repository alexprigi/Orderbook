/// <reference path="index.d.ts" />

export function sortDescending(number1: number, number2: number): number {
    return number2 - number1;
};

export function sortAscending(number1: number, number2: number): number {
    return number1 - number2;
};